# 新版逐格 Qwen 首局

种子 20260920；真实本地 Qwen3.5-0.8B MLX 4-bit，Jev 兼容 logits 选择接口。每次有效动作后施加一格重力，无踢墙；每局上限 10000 动作，本局自然出生受阻结束。

|指标|结果|
|---|---:|
|动作次数|63|
|锁定方块数|7|
|消行|0|
|平均 HTTP 响应|163.2 ms|
|HTTP 耗时合计|10.28 秒|
|下落 / 旋转 / 左移 / 右移|58 / 5 / 0 / 0|

63 次决策都提供了至少一个合法横移动作，模型从未选中；所有方块保持出生列附近，中央堆高后下一块 O 无法出生。未发生接口失败或非法动作。此局只能说明当前模型、提示词和逐动作输入下的表现，不能证明所有逐格策略都更差。新版按中央出生位置碰撞结束，旧版按所有顶部硬降候选为空结束；规则与一步定义不同，不能直接比较总动作数。

通过 dart run tool/verify_step_run.dart 独立重建 63 次请求、活动方块、棋盘、消行和终局。动作及完整 API 原文在 actions.jsonl，指标在 metrics.json。此次在同一 Dart 引擎的评测器运行，未通过网页逐次点击，也未更改页面当前对局。
