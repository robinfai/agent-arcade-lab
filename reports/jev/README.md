# JEV 接入验证

2026-09-21，通过本地 8770 代理调用官方 `https://api.typesafe.ai/v1/systemone`。密钥来自 `JEV_API_KEY`，未写入文件。实际模型返回 `jev-1.13.0`。

`dart run tool/check_jev.dart` 完成 10 次真实请求：

- 俄罗斯方块：3 个合法落点，累计执行 39 个动作，0 分，覆盖普通选择与直接落位接口。
- 贪吃蛇独玩：5 帧，0 分，使用绝对方向候选。
- 贪吃蛇竞技：JEV 双方各请求一次，同帧结算 1 帧，使用相对方向候选。

全部执行原始模型选择，关闭执行纠错；候选中的程序提示保留。以上是接入冒烟检查，未进行完整对局或成绩比较。

原始请求、响应、概率和执行摘要：`smoke.json`。重跑会覆盖该文件。

代理契约测试 11 项通过，覆盖三种接口、缺少密钥、无效请求、鉴权失败、限流、畸形响应、超时及本地跨域请求。上游失败响应不直接回传，避免泄露敏感信息。

Flutter 测试：47 项通过。Web 构建通过。`flutter analyze --no-fatal-infos` 通过，无 error/warning；仍有既存评测脚本的 3 条 `curly_braces_in_flow_control_structures` 提示（严格 `flutter analyze` 会因此返回非零）。

浏览器实测：模型选择切到 JEV，俄罗斯方块点击“AI 直接落位”成功，模型原始/执行均为 p15，纠错 0 次，等价 20 动作，HTTP 812 ms。

已核对密钥未出现在代理源码、浏览器构建产物和冒烟报告中。

浏览器贪吃蛇独玩选择 JEV，点击“走一帧”成功：原始/执行均为 DOWN，纠错 0 次，帧数从 0 到 1，HTTP 897 ms。两款游戏均完成浏览器到代理、云端决策、棋盘执行的闭环；未运行完整自动对局。
