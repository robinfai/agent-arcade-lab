DECISION CHECKLIST (use a short calculation, not repeated strategic prose):
1. Coordinates are ZERO-BASED. The last board string is row 19. In "...#......" the # is column 3, not 2. In each cell pair [dx,dy], FIRST is horizontal and SECOND is vertical. [[0,0],[0,1],[0,2],[0,3]] is VERTICAL: one column, four rows. Absolute column = active.column + dx; absolute row = active.row + dy. Never swap these.
2. Trust the supplied first-step outcomes: locks_piece=true means this action locks NOW at row_after_step. It does not mean a good placement. If locking near rows 0/1 blocks the next spawn, reject that action when a non-locking escape is available. Find the escape from the legal action outcomes before choosing down.
3. Look for a nearly full row. If a vertical piece is adjacent to its single-cell gap, move TOWARD that gap before descending; moving away or rotating can lose the clear. A smaller target column requires left, a larger target requires right. Check actual cells, do not assume down is safe.
4. Choose the first action using those checks, then simulate subsequent inputs. Prefer clearing rows and preserving spawn space. Down alone earns no points. Once aligned with a useful landing, repeated down is appropriate.
Keep reasoning concise: actual occupied cells, immediate danger or gap, selected first action and follow-up. Do not repeat analysis. Finish thinking and call play_turn.
