# 逐格下落实现验证

- flutter analyze：通过。
- flutter test：11 项通过；包含逐格重力、途中移动旋转、墙与堆叠碰撞、无踢墙、滑入遮挡下方、落地锁定、四行消除、出生碰撞结束和页面按钮操作。
- flutter build web：通过。
- 真实 MLX 接口：种子 20260920，20 次合法动作，锁定 1 块，达到动作测试上限，未跑完整一局。完整请求/响应见 actions.jsonl。
- 浏览器：手动左移、下落、旋转、右移后动作计数为 4，活动 I 在第 5 行呈竖直姿态，尚未锁定；AI 单步返回 down，计数变为 5，HTTP 190 ms。
- 未运行新版十局能力评测或 macOS 原生构建。

规则：每次有效输入后施加一格重力，受阻锁定；不输入则暂停。旋转碰撞拒绝，不自动踢墙。旧版硬降引擎与实验保留，页面标识为旧版记录。
