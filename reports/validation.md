# 验证记录 · 2026-09-20

- `flutter analyze`：通过，No issues found。
- `flutter test`：6 项通过，覆盖旋转、七袋种子一致性、四消与计分、非法操作不改变棋盘、碰撞/单步方块数量守恒、界面手动硬降。
- `flutter build web`：通过。
- `.venv/bin/python -m pytest backend/test_contract.py -q`：5 项通过，覆盖三类输出语义与无效问题拒绝。
- TypeSafe 官方 SDK 0.7.0 对本地真实 MLX 服务调用 choice/noul/score：成功；苹果颜色选择 red，响应已保存到 sdk-check.json。
- 十局真实 API 评测完成：254 条步骤记录，无失败重试、无非法动作、无规则回退。每次响应均检查候选键集合和概率和。
- 浏览器访问 `http://127.0.0.1:18787`：页面加载并显示全部十局数据；点击 AI 单步后步骤从 0 增至 1，显示 r0x0 / 472 ms；自动玩推进到第 5 步后点击暂停恢复 AI 自动玩按钮；第二局回放正常启动。
- 本次使用 Flutter Web，不声称已构建或测试 macOS 原生二进制。
- 无 TypeSafe 云端凭据调用、无云端 Jev 质量或性能比较、无概率校准测量。

本地进程：MLX API 127.0.0.1:8765，Flutter Web 静态预览 127.0.0.1:18787。为用户查看交付而保留运行。

## 手动基准补充

- 新的模型试玩已按用户指令暂停。
- `dart run tool/verify_manual.dart`：114 个动作逐帧重放通过；29 行、4,300 分，下一块 L 无合法落点。
- 手动基准回放资产加入后，`flutter analyze` 无问题，6 项 Flutter 测试通过。
- 手动局实际执行方式为显式 Dart 落点命令，不声称浏览器逐次按键玩完。
- 最终 Web 构建通过；浏览器显示“手动基准 / 114 步 / 29 行 / 堆顶”，点击“回放手动基准”后进入 114 帧回放，HTTP 耗时与模型置信度显示为“—”，未触发模型请求。
